NODE_ENV=local
DATABASE_URL="postgresql://user:password@your-neon-host.neon.tech/your-database?sslmode=require"
API_KEY="your_api_key"

# On Vercel, set DATABASE_URL and API_KEY in the project's Environment Variables settings.
# NODE_ENV will be "production" by default on Vercel.